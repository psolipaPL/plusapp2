import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'labs.productleague.mobile',
  appName: 'PLUS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
